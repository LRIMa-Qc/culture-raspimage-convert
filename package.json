{
  "name": "culture-raspimage-convert",
  "version": "0.1.0",
  "type": "commonjs",
  "main": "index.js",
  "bin": {
    "culture-raspimage-convert": "bin/cli.js"
  },
  "files": [
    "index.js",
    "bin/cli.js",
    "src",
    "config_file",
    "Cargo.toml",
    "Cargo.lock"
  ],
  "scripts": {
    "build": "cargo build --release",
    "prepare": "cargo build --release",
    "install": "cargo build --release"
  }
}
